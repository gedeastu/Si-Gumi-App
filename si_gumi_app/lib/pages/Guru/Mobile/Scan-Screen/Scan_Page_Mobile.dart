import 'package:flutter/material.dart';

class ScanPageMobile extends StatelessWidget {
  const ScanPageMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Welcome to Scan"),
    );
  }
}